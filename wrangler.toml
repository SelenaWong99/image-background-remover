name = "image-background-remover"
compatibility_date = "2024-09-23"
compatibility_flags = ["nodejs_compat"]
pages_build_output_dir = ".next"

# Rate Limiting binding 在 Cloudflare Pages Dashboard 里配置：
# Settings → Functions → Rate limiting bindings → Add binding
# Variable name: RATE_LIMITER
# Limit: 3 requests per 86400 seconds (per IP)
